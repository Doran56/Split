# URL publique du backend /server déployé sur Vercel.
# Valeur non secrète : les variables EXPO_PUBLIC_* sont de toute façon intégrées
# en clair dans le bundle client par Expo, donc ce fichier peut être commité.
EXPO_PUBLIC_API_BASE_URL=https://split-teal-rho.vercel.app
